identity:
  name: "sql-clean"
  author: "covfefe"
  label:
    en_US: "SQL Clean"
    zh_Hans: "SQL 清理"
    pt_BR: "SQL Clean"
    ja_JP: "SQL Clean"
description:
  human:
    en_US: "Remove ```sql``` fences and newlines from SQL"
    zh_Hans: "去除 SQL 文本中的 ```sql``` 及换行"
    pt_BR: "Remove ```sql``` fences and newlines from SQL"
    ja_JP: "Remove ```sql``` fences and newlines from SQL"
  llm: "Clean SQL by removing code fences and newlines"
parameters:
  - name: sql
    type: string
    required: true
    label:
      en_US: SQL text
      zh_Hans: SQL 文本
      pt_BR: SQL text
      ja_JP: SQL text
    human_description:
      en_US: "SQL text to clean"
      zh_Hans: "需要清理的 SQL 文本"
      pt_BR: "SQL text to clean"
      ja_JP: "SQL text to clean"
    llm_description: "SQL text to clean"
    form: llm
extra:
  python:
    source: tools/sql-clean.py